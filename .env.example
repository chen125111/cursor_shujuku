# ================================
# 气体混合物数据管理系统 - 环境变量示例
# 复制为 .env 后按需修改：
#   cp .env.example .env
# ================================

# --- 认证与安全 ---
# 管理员用户名（可选，默认 admin）
ADMIN_USERNAME=admin

# 管理员密码（必须设置，否则管理员登录会被禁用）
ADMIN_PASSWORD=ChangeMe_StrongPassword

# JWT 签名密钥（生产环境必须设置为随机强密钥）
SECRET_KEY=ChangeMe_RandomLongSecretKey

# --- 数据库 ---
# SQLite 文件路径（Docker Compose 已在容器内设置，一般无需改）
# DATABASE_PATH=gas_data.db
# SECURITY_DB_PATH=security.db

# 如需使用 MySQL，请设置连接字符串（示例）：
# DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/gas_db
# SECURITY_DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/gas_security_db

# --- 备份 ---
BACKUP_DIR=backups

# --- CORS ---
# 多个来源用逗号分隔，例如：
# CORS_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
CORS_ORIGINS=

# --- Redis（可选）---
# REDIS_URL=redis://127.0.0.1:6379/0
# REDIS_PREFIX=gasapp

